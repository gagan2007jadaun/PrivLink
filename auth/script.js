// Frontend-only placeholder
// No backend, no validation yet

document.querySelector(".primary-btn").onclick = () => {
    alert("Frontend only UI. Backend auth will be added later.");
};

document.querySelector(".ghost-btn").onclick = () => {
    alert("Anonymous mode UI. Backend will handle this later.");
};
